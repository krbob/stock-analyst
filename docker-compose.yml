services:
  stock-analyst:
    image: ${STOCK_ANALYST_IMAGE:-stock-analyst:local}
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    depends_on:
      stock-analyst-backend-yfinance:
        condition: service_healthy
    environment:
      - BACKEND_URL=http://stock-analyst-backend-yfinance:8081
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8080/health"]
      interval: 10s
      timeout: 3s
      retries: 12
      start_period: 10s
    restart: unless-stopped

  stock-analyst-backend-yfinance:
    image: ${STOCK_ANALYST_BACKEND_YFINANCE_IMAGE:-stock-analyst-backend-yfinance:local}
    build:
      context: ./backend-yfinance
      dockerfile: Dockerfile
    environment:
      YFINANCE_HISTORY_CACHE_MAX_BYTES: ${YFINANCE_HISTORY_CACHE_MAX_BYTES:-67108864}
      YFINANCE_HISTORY_CACHE_MAX_ENTRIES: ${YFINANCE_HISTORY_CACHE_MAX_ENTRIES:-512}
      YFINANCE_METADATA_CACHE_MAX_BYTES: ${YFINANCE_METADATA_CACHE_MAX_BYTES:-8388608}
      YFINANCE_METADATA_CACHE_MAX_ENTRIES: ${YFINANCE_METADATA_CACHE_MAX_ENTRIES:-2048}
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8081/health"]
      interval: 10s
      timeout: 3s
      retries: 12
      start_period: 10s
    restart: unless-stopped
